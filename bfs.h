#include "Star.h"
#include <vector>


class BFS {
    public:
    BFSsmall(Star* star); // traversal from one start point

    BFStraversal(const std::vector<Star>& star_list_); // call bfs small on every vertex



    canTravel(Star* start, Star* end);






};